# Source tmux reset configuration
source-file ~/.config/tmux/tmux.reset.conf

# --- KITTY CURSOR & COLOR PERSISTENCE ---
# Use tmux-256color for proper terminfo support
set-option -g default-terminal "tmux-256color"

# Enable True Color (RGB) support for Kitty
set-option -ag terminal-features ",xterm-kitty:RGB"
set-option -ag terminal-overrides ",xterm-kitty:Tc"

# DISABLE tmux cursor management. 
# By setting Ss and Se to empty, tmux stops sending "reset" commands.
# This forces tmux to keep the EXACT color and shape defined in kitty.conf.
set-option -ag terminal-overrides ',xterm-kitty:Ss=:Se='

# --- BASIC SETTINGS ---
set -g prefix C-a
set -g base-index 1
set -g detach-on-destroy off
set -g escape-time 0
set -g history-limit 1000000
set -g renumber-windows on
set -g status-position top
setw -g mode-keys vi
set -g display-time 1000
set -g set-clipboard on
set -s copy-command 'xclip -selection clipboard'
set -g mouse on

# --- STYLING & COLORS ---
set -g pane-border-lines simple
set -g pane-border-style 'fg=colour255'
set -g pane-active-border-style 'fg=#1b668f'

# Popups and Menus
set -gF popup-style "bg=default,fg=#1b668f"
set -gF popup-border-style "fg=#1b668f"
set -gF menu-selected-style "bg=#1b668f,fg=#E4F0FB"

# Mode Style (Selection in copy mode)
set -g mode-style "bg=#1b668f,fg=#E4F0FB,bold"

# Message Styles
set -g message-style "bg=#1b668f,fg=#E4F0FB"
set -g message-command-style "bg=#D0679D,fg=#FAE4FC"

# --- STATUS BAR ---
set -g status-bg '#212124'
set -g status-fg '#506477'

set -g window-status-format "#[fg=white,bg=black] #I:#W "
set -g window-status-current-format "#[fg=#E4F0FB,bg=#1b668f,bold] #I:#W "

set -g status-left '#[fg=#E4F0FB,bg=#D0679D,bold] [#S] '
set -g status-right ''

# --- PLUGINS (TPM) ---
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

set -g @resurrect-strategy-nvim 'session'
set -g @continuum-restore 'on'
set -g @continuum-save-interval '10'

# Initialize TPM
run '~/.tmux/plugins/tpm/tpm'
